// Archived implementation moved to archive/legacy-ai/flows/generate-audio-from-text.ts
// Runtime stub to prevent accidental execution of genkit flows. Use `ai_server` for AI features.
export async function generateAudioFromText(_input: string) {
  return { audioDataUri: '' };
}
